# python-backend-ledger
This is the Python Backend which will help the ledger application with backend services

To run this application execute the following command.

python -m uvicorn main:app --reload